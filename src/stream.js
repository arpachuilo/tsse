// import * as actions from './actions'
// import io from 'socket.io-client'

// import config from './config'

// Provides a way during startup of application to bind
//  poll/listen methods to the redux store for easy access
export default (store) => {
  // const server = config.app.socketUrl
  // const socket = io.connect(server)

  // Listen
  // socket.on('event', () => {
  //
  // })

  // Poll
  // const updateInterval = 1000
  // setInterval(() => {
  //
  // }, updateInterval)
}
